# 智能空间开发续接记录（2026-07-20 09:26）

## 当前已完成的代码

- 左侧文件夹右键支持“取消显示（仅隐藏左栏）”和“取消索引此文件夹”。
- 设置 → 索引范围新增排除文件夹和恢复索引；左栏隐藏文件夹可恢复显示。
- 设置 → 文件格式新增索引格式白名单、匹配文件显示格式，两者独立配置。
- 文件格式项已补为明确的“✔ 已选 / ○ 未选”，整项可点击切换。
- 索引器会在目录遍历阶段剪枝排除目录，并按格式白名单跳过不需要的文件。
- 预览按钮使用固定布局，在五档密度和长文件说明下保持可见。
- 文件夹与下钻列表已经与文件列表共用五档密度。
- AI 筛选按索引候选返回相关文件，底部新增可滚动的 AI 分析结论和“向下翻页”按钮。
- 输入搜索使用 260 ms 防抖，避免每个按键都刷新文件夹和文件列表。
- 主界面改为左侧七入口操作轨（图标/置顶/隐藏/全量/增量/暂停/设置），搜索、AI、空间和类型合并为单行。
- 设置页去掉重复大标题，目录、排除和恢复按钮放到对应小标题右侧。

## 已完成验证

- `build-clean` 编译成功。
- Python 索引测试共 16 项通过，包含排除目录和格式白名单测试。
- 修改“✔/○”前的一轮完整 UI 冒烟测试通过，共生成 27 张截图；隐藏、排除、AI、预览、五档密度等均通过。
- 最新截图人工检查：AI 结论区、AI 设置、隐藏文件夹、排除文件夹视觉正常。

## 续接完成状态

1. 最新“✔/○”格式页已完成，列表与按钮不再重叠。
2. 新增左侧操作轨，集中放置置顶与隐藏；贴边星标与隐藏按钮同尺寸、同轴定位。
3. 隔离 Xvfb + D-Bus 的 27 张 UI 截图测试通过。
4. CTest 4/4、严格警告编译和 25,000 文件资源基准全部通过。
5. 已安装到 `~/.local` 并重启；D-Bus 可用、智能空间可见、桌面图标零重叠、无自动索引进程。
6. 实机贴边隐藏几何从 `0,440,919,519` 变为 `0,503,46,42`，星标顶边与原隐藏按钮顶边一致；展开后完整恢复原几何。
7. 实机 5 秒稳态 CPU 为 0.6%；正式索引与断点在安装前后 SHA-256 一致。

## 索引断点

- 全量索引当前未运行。
- 断点文件保留在：`~/.cache/kylin/ukui-fences/smart-space/index.json.resume.jsonl`
- 暂停时断点大小约 22.46 MB，时间为 2026-07-20 07:11。
- 正式索引 `index.json` 未被测试覆盖。
- 安装重启时不得删除断点，也不要自动恢复全量任务；由用户配置完排除目录和格式后手动继续。
